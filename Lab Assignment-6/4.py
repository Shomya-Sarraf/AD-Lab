# 4. Write code to compute prior, likelihood, and posterior probabilities for
# a given dataset.
prior = 0.5
likelihood = 0.8
evidence = 0.6
posterior = (likelihood * prior) / evidence
print(posterior)
print("Shomya Sarraf, 23053668")
